#pragma config(UART_Usage, UART2, uartNotUsed, baudRate4800, IOPins, None, None)
#pragma config(Sensor, in1,    linefollow,     sensorLineFollower)
#pragma config(Sensor, dgtl1,  sortenc1,       sensorQuadEncoder)
#pragma config(Sensor, dgtl11, startsw,        sensorTouch)
#pragma config(Motor,  port1,           sortmotor,     tmotorVex393_HBridge, openLoop)
#pragma config(Motor,  port2,           light,         tmotorVexFlashlight, openLoop, reversed)
#pragma config(Motor,  port3,           indexservo,    tmotorServoStandard, openLoop)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

//Servo Value Constants
int grabmarble = 12;
int dropmarble = 12;

//Encoder Position Values
int metalbox = 1;
int woodbox = 2;
int clearbox = 3;
int opaquebox = 4;

//Marble Line Follow Value Constants
int metal = 1;
int wood = 2;
int clear = 3;
int opaque = 4;

task main()
{
	setServo(indexservo, dropmarble);
	if(SensorValue[startsw]){
		setServo(indexservo, grabmarble);
		wait(10);
		setServo(indexservo, dropmarble);

		//Begin Marble Type Determnination
		if(SensorValue[linefollow] == metal){
			if(SensorValue[sortenc1] < metalbox){
				repeatUntil(SensorValue[sortenc1] == metalbox){
					startMotor(sortmotor, 100);
				}
				stopMotor(sortmotor);
			}
			if(SensorValue[sortenc1] > metalbox){
				repeatUntil(SensorValue[sortenc1] == metalbox){
					startMotor(sortmotor, -100);
				}
				stopMotor(sortmotor);
			}
		}
		if(SensorValue[linefollow] == wood){
			if(SensorValue[sortenc1] < woodbox){
				repeatUntil(SensorValue[sortenc1] == woodbox){
					startMotor(sortmotor, 100);
				}
				stopMotor(sortmotor);
			}
			if(SensorValue[sortenc1] > woodbox){
				repeatUntil(SensorValue[sortenc1] == woodbox){
					startMotor(sortmotor, -100);
				}
				stopMotor(sortmotor);
			}
		}
		if(SensorValue[linefollow] == clear){
			if(SensorValue[sortenc1] < clearbox){
				repeatUntil(SensorValue[sortenc1] == clearbox){
					startMotor(sortmotor, 100);
				}
				stopMotor(sortmotor);
			}
			if(SensorValue[sortenc1] > clearbox){
				repeatUntil(SensorValue[sortenc1] == clearbox){
					startMotor(sortmotor, -100);
				}
				stopMotor(sortmotor);
			}
		}
		if(SensorValue[linefollow] == opaque){
			if(SensorValue[sortenc1] < opaquebox){
				repeatUntil(SensorValue[sortenc1] == opaquebox){
					startMotor(sortmotor, 100);
				}
				stopMotor(sortmotor);
			}
			if(SensorValue[sortenc1] > opaquebox){
				repeatUntil(SensorValue[sortenc1] == opaquebox){
					startMotor(sortmotor, -100);
				}
				stopMotor(sortmotor);
			}
		}
	}
}
